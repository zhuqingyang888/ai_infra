# PyTorch Forward Inference Project

这是一个用于练习 PyTorch 前向推理的小项目。

## 项目内容

这个项目实现了以下功能：

- 随机生成输入
- 定义一个简单 MLP
- 将模型放到 CPU 或 GPU
- 执行前向推理
- 统计推理耗时
- 比较不同 batch size

## 文件说明

- `practice.py`：主推理脚本
- `README.md`：项目说明文件

## 运行方式

### 1. 安装依赖
```bash
pip install torch